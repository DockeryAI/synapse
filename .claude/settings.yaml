# Claude Code Project Settings
# Auto-generated by BuildRunner 3

# Use opusplan for automatic model switching
# - Opus during planning phases (best reasoning)
# - Sonnet during execution (fast, capable)
model: opusplan

# Optional: Override for specific use cases
# model: opus    # Always use Opus (expensive but best)
# model: sonnet  # Always use Sonnet (balanced)
# model: haiku   # Always use Haiku (fast, cheap)
